@clearignore 
if listexists bags
    removelist bags
endif

createlist bags
clearlist bags

if findtypelist bags 'bag' self
    foreach x in bags
        overhead "Opening {{x}}"
        lift x
        wait 700
        drop self
        while queued
            wait 700
        endwhile
    endfor
endif
